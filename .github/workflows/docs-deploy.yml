name: DocumentationBuildDeploy

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install pip
        run: python -m pip install --upgrade pip
      
      - name: Install mkdocs
        run: |
          pip install mkdocs
          pip install mkdocs-material
      
      - name: Build docs
        run: mkdocs build
        
      - name: Install lftp
        run: sudo apt install lftp

      - name: FTP Deploy
        run: |
          echo Starting...
          lftp -c "open -u thingieq,${{ secrets.QS_FTP_PWD }} win19.mojsite.com; mirror -R site querystorm.com/wwwroot/DocsTest"
          echo Done transferring files.
